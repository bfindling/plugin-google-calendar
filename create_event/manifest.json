{
  "name": "create_event",
  "description": "Create a new Google Calendar event.",
  "entrypoint": "run.py",
  "parameters": {
    "title": {
      "type": "string",
      "description": "Event title."
    },
    "start": {
      "type": "string",
      "description": "Start time in RFC3339 format (e.g. 2025-06-15T10:00:00Z) or date for all-day events (e.g. 2025-06-15)."
    },
    "end": {
      "type": "string",
      "description": "End time in RFC3339 format or date for all-day events."
    },
    "description": {
      "type": "string",
      "description": "Optional event description."
    },
    "location": {
      "type": "string",
      "description": "Optional event location."
    },
    "attendees": {
      "type": "string",
      "description": "Optional comma-separated list of attendee email addresses (e.g. 'alice@example.com, bob@example.com'). Pass 'none' to explicitly send an empty attendees list. Omit this parameter entirely to leave attendees unset. An empty string is an error."
    }
  }
}
